#include <bits/stdc++.h>

using namespace std;

#define MAX(a, b) ( (a > b ) ? a : b )
#define MIN(a, b) ( (a < b ) ? a : b )

#define LIMIT 100000

//  ***** MAIN *****
int main(){
  //ios::sync_with_stdio(false);
  cin.tie(NULL);
  int n; cin >> n;
  int v[LIMIT] = {0};
  int d[LIMIT] = {0};

  for (int i = 0; i < n; i++) {
    cin >> v[i];
  }

  for (int i = 0; i < 320; i++) {

  }


  return 0;
}

