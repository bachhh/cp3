#include <bits/stdc++.h>

using namespace std;

#define LIMIT 100

//  ***** MAIN *****
int main(){
  //ios::sync_with_stdio(false);
  cin.tie(NULL);

  int tc; cin >> tc;
  int n;
  string s;
  while(tc--){
    cin >> n;
    cin >> s;
    vector<vector<int>> matrix(s.size(), vector<int>(s.size(), 0));
  }


  return 0;
}

