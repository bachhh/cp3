#include <bits/stdc++.h>

using namespace std;

#define LIMIT 100

//  ***** MAIN *****
int main(){
  //ios::sync_with_stdio(false);
  cin.tie(NULL);
  string w;
  string tg = "CODEFORCES";
  cin >> w;

  for (int i = 1; i < tg.size(); i++) {
    string a = tg.substr(0,i);
    string b = tg.substr(i);
    if (x != 0 && y == )
  }


  return 0;
}

