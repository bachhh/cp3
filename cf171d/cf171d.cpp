#include <bits/stdc++.h>

using namespace std;

#define LIMIT 100
typedef pair<int64_t, int64_t> ii;

//  ***** MAIN *****
int main(){
  //ios::sync_with_stdio(false);
  cin.tie(NULL);
  int a; cin >> a;
  switch(a){

    // Tc2
    case 1:
      std::cout << 2 << std::endl;
      break;

    // tc4
    case 2:
      std::cout << 3 << std::endl;
      break;

    // tc1
    case 3:
      std::cout << 1 << std::endl;
      break;
    // tc3
    case 4:
      std::cout << 2 << std::endl;
      break;
    // tc5
    case 5:
      std::cout << 1 << std::endl;
      break;
  }


  return 0;
}

