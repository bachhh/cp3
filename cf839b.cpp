#include <bits/stdc++.h>

using namespace std;

#define LIMIT 100

//  ***** MAIN *****
int main(){
  //ios::sync_with_stdio(false);
  cin.tie(NULL);
  int n, k;
  int a[100];

  cin >> n >> k;
  int odd =0;

  int rem = n*8;
  for (int i = 0; i < k; i++) {

    cin >> a[i];
    while(a[i] > 0){
    }

  }


  return 0;
}

