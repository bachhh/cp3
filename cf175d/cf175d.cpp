#include <bits/stdc++.h>

using namespace std;

#define LIMIT 100
typedef pair<int64_t, int64_t> ii;

//  ***** MAIN *****
int main(){

  //ios::sync_with_stdio(false);
  cin.tie(NULL);

  int hp, dt, l, r, p;
  int ehp, edt, el, er, ep;

  cin >> hp >> dt >> l >> r >> p;
  cin >> ehp >> edt >> el >> er >> ep;

  return 0;
}

